import { Routes } from '@angular/router';
import { ShipmentTrackerContainerComponent } from './shipment-tracker-container/shipment-tracker-container.component';

export const SHIPMENT_TRACKER_ROUTES: Routes = [
  { path: '', component: ShipmentTrackerContainerComponent },
];
